import { TeamFreelancerMessage } from './team-freelancer-message';

describe('TeamFreelancerMessage', () => {
  it('should create an instance', () => {
    expect(new TeamFreelancerMessage()).toBeTruthy();
  });
});
