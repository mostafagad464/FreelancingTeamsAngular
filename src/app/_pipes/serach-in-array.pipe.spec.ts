import { SerachInArrayPipe } from './serach-in-array.pipe';

describe('SerachInArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SerachInArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
